# idea because Colin has to use an IDE
.idea

# YASS generator for local runs
generate_site.py

# since most of docs/ is generated on-the-fly, ignore everything,
docs/*

# except:
!docs/404.html
!docs/CNAME
!docs/static

# but do ignore this one symlink lol
docs/static/assets